using static System.Console;
using static CsharpBasics.Exercises;
using static CsharpBasics.SimpleCalculator;
using static CsharpBasics.L1_Datatypes;
using static CsharpBasics.L2_Variables_and_Operators;
using static CsharpBasics.L3_Control_Structures;
using static CsharpBasics.L4_Object_Orientation;

// See https://aka.ms/new-console-template for more information

//// PLAYGROUND:

schleifen(true);

//variablen();

//binaerzahlRechner();

//int a = 0;
//int b = 0;
//int c = 0;
//int d = 0;

//schaltjahrRechner();

//WriteLine(a + "ist a, " + b + " ist b," + c + " ist c, " + d + " ist d");
//WriteLine($"{a} ist a, {b} ist b, {c} ist c, {d} ist d");


//schaltjahrRechner();



//// AUSGABE:
WriteLine(""); // Konsolenausgabe mit Absatz.

Write(""); // Konsolenausgabe ohne Absatz.

//// EINGABE:
Read(); // Konsoleneingabe für ein Zeichen (char).

ReadLine(); // Konsoleneingabe für eine Zeichenkette (string). Um zur nächsten Anweisung zu springen muss ein Absatz gegeben werden.
